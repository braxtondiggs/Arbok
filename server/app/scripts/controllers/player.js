'use strict';

/**
 * @ngdoc function
 * @name MVPlayer.controller:PlayerCtrl
 * @description
 * # PlayerCtrl
 * Controller of the MVPlayer
 */
angular.module('MVPlayer')
  .controller('PlayerCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
